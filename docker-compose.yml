services:
  postgres:
    image: postgres:16
    ports:
      - "5432:5432"
    environment:
      POSTGRES_DB: interop
      POSTGRES_USER: interop
      POSTGRES_PASSWORD: interop
    volumes:
      - ./db/init.sql:/docker-entrypoint-initdb.d/init.sql:ro
